# Frontend snapshot provenance

The production/development frontend reviewed here comes from the private Green Sheet `showdown-v8-cleanup` branch and is currently a large single-file `index.html` (~189 KB) containing HTML, CSS and client-side JavaScript.

The public sandbox intentionally contains no deployment workflow, Cloudflare configuration or secrets. This note exists so reviewers know the website source and the live product should be evaluated together.

Live product: https://green-sheet.greensheet-hq.workers.dev/
